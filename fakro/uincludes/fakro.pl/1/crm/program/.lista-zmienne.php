lista dostepnych zmiennych:<br><br>

{loop:wyniki} <- poczatek tablicy<br><br>
&lt;!-- loop:program --&gt; <- poczatek petli<br><br>
kameleon:inside_link(643){next} <- link do nastepnej strony (dotyczy strony 625)<br>
{id_pl_programy} <- zmienna zwraca id programu (zmienna dostepna tylko w petli)<br>
{nazwa} <- zmienna zwraca nazwe programu (zmienna dostepna tylko w petli)<br>
&lt;!-- endloop:program --&gt; <- koniec petli<br><br><br>
{liczba_punktow} <- zmienna zwraca liczbe punktow w programie<br>
{wydane_punkty} <- zmienna zwraca liczbe wydanych punktow w programie<br>
{liczba_wydan} <- zmienna zwraca liczbe wydan w programie<br>
{roznica_punktow} <- zmienna zwraca roznice punktow w programie<br><br>
{endloop:wyniki} <- koniec tablicy<br>